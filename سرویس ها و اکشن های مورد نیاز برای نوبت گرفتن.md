
ماژول appointments (رزرو و نوبت‌دهی)

Services:

get_available_slots_service.py (GetAvailableSlotsService): محاسبه اسلات‌های آزاد بر اساس طول خدمت و ساعات کاری.

create_appointment_service.py (CreateAppointmentService): درج رکورد نوبت جدید در دیتابیس.

get_appointments_list_service.py (GetAppointmentsListService): کوئری و دریافت لیست نوبت‌ها (با قابلیت فیلتر بر اساس آرایشگر، مشتری، تاریخ و وضعیت).

update_appointment_status_service.py (UpdateAppointmentStatusService): تغییر مستقیم status نوبت در دیتابیس.

Actions:

book_appointment_action.py (BookAppointmentAction): ارکستراسیون فرایند رزرو (چک کردن مجدد خالی بودن اسلات، محاسبه قیمت نهایی و فراخوانی سرویس ثبت).

change_appointment_status_action.py (ChangeAppointmentStatusAction): بررسی سطوح دسترسی (آرایشگر/مشتری) و اعتبارسنجی منطق تغییر وضعیت نوبت (مثلاً عدم تغییر نوبت لغوشده).

cancel_appointment_action.py (CancelAppointmentAction): لغو نوبت و اعمال منطق‌های جانبی (نظیر آزادسازی زمان).

ماژول barbers (مدیریت آرایشگران و خدمات اختصاصی)

Services:

get_barber_services_service.py (GetBarberServicesService): دریافت لیست خدمات فعال یک آرایشگر همراه با قیمت و زمان اختصاصی.

upsert_barber_service_service.py (UpsertBarberServiceService): ثبت یا به‌روزرسانی خدمت اختصاصی (BarberServiceModel).

get_working_hours_service.py (GetWorkingHoursService): دریافت برنامه زمان‌بندی هفتگی آرایشگر.

update_working_hours_service.py (UpdateWorkingHoursService): ثبت یا ویرایش ساعات کاری در WorkingHoursModel.

Actions:

manage_barber_services_action.py (ManageBarberServicesAction): دریافت ورودی‌های فرم و اعمال تغییرات روی کاتالوگ خدمات آرایشگر.

save_working_hours_action.py (SaveWorkingHoursAction): دریافت فرم ساعات کاری هفته و ذخیره یکجای آن برای روزهای مختلف.

ماژول salon_services (خدمات پایه سالن)

Services:

get_active_services_service.py (GetActiveServicesService): دریافت لیست کل خدمات پایه و فعال سالن.

create_salon_service_service.py (CreateSalonServiceService): تعریف خدمت جدید پایه در سیستم.

Actions:

manage_salon_services_action.py (ManageSalonServicesAction): ارکستراسیون ایجاد و ویرایش خدمات سالن توسط مدیر.

ماژول accounts (احراز هویت و کاربران)

Services:

authenticate_user_service.py (AuthenticateUserService): بررسی صحت نام کاربری و رمز عبور.

create_user_profile_service.py (CreateUserProfileService): ساخت حساب کاربر به همراه پروفایل مربوطه (CustomerModel یا BarberModel).

Actions:

user_login_action.py (UserLoginAction): لاگین کاربر و هدایت بر اساس نقش (ROLE).

user_register_action.py (UserRegisterAction): ثبت‌نام کاربر و تخصیص نقش اولیه.